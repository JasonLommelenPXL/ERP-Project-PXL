/* ==============================
   Minimal ERP-styling (full)
   ============================== */

/* Base reset */
*, *::before, *::after {
    box-sizing: border-box;
}

html, body {
    height: 100%;
    margin: 0;
}

/* Theme tokens */
:root {
    /* Cards */
    --erp-card-bg: #f7f9fc;
    --erp-card-border: #eef2f7;
    --erp-text: #111827; /* iets donkerder voor betere leesbaarheid */
    --erp-muted: #6b7280;
    --erp-radius: 14px;
    --erp-shadow: 0 6px 20px rgba(0,0,0,.06);
    --icon-size: 56px;
    --icon-radius: 12px;
    --erp-card-min-h: 120px; /* overal gelijke hoogte – stel bij (118–124) */
    /* Shell */
    --sidebar-w: 260px;
    --sidebar-bg: #f3f4f6;
    --sidebar-text: #111827;
    --sidebar-muted: #6b7280;
    --sidebar-active: rgba(0,0,0,.06);
    --sidebar-border: #e5e7eb;
    --topbar-bg: #1f2937;
    --topbar-text: #f9fafb;
    --topbar-text-muted: #e5e7eb;
    --topbar-h: 56px;
    --content-bg: #ffffff;
}

/* ============= Shell/Layout ============= */
.container-fluid.erp-shell {
    padding-left: 0 !important;
    padding-right: 0 !important;
}

.erp-shell {
    display: flex;
    align-items: stretch;
    min-height: calc(100vh - var(--topbar-h)); /* onder de topbar */
    background: var(--sidebar-bg);
}

/* Topbar: gebruik slechts één .navbar in je partial (zie opmerking onderaan) */
.erp-topbar {
    background: var(--topbar-bg) !important;
    color: var(--topbar-text) !important;
    min-height: var(--topbar-h);
    margin: 0 !important;
}

    .erp-topbar .navbar-brand,
    .erp-topbar .nav-link {
        color: var(--topbar-text) !important;
    }

        .erp-topbar .nav-link:hover {
            color: var(--topbar-text-muted) !important;
        }

/* Sidebar */
.erp-sidebar {
    width: var(--sidebar-w);
    flex: 0 0 var(--sidebar-w);
    background: var(--sidebar-bg);
    color: var(--sidebar-text);
    border-right: 1px solid var(--sidebar-border);
    /* sticky binnen de viewport onder de topbar */
    position: sticky;
    top: var(--topbar-h);
    height: calc(100vh - var(--topbar-h));
    overflow: auto;
    -webkit-overflow-scrolling: touch;
}

/* Brand + nav */
.erp-brand {
    padding: 1rem 1rem .25rem 1rem;
    font-weight: 700;
    display: flex;
    align-items: center;
    gap: .5rem;
    color: var(--sidebar-text);
}

.erp-nav {
    padding: .25rem .5rem 1rem .5rem;
}

.erp-navsection {
    margin: .75rem .5rem .25rem;
    padding: .25rem .25rem;
    font-size: .75rem;
    letter-spacing: .02em;
    color: var(--sidebar-muted);
    text-transform: uppercase;
}

/* Links in de sidebar */
.erp-sidelink {
    display: flex;
    align-items: center;
    gap: .625rem;
    padding: .625rem .75rem;
    margin: .125rem .25rem;
    border-radius: 10px;
    color: var(--sidebar-text);
    text-decoration: none;
    transition: background-color .15s ease, color .15s ease, transform .05s ease;
}

    .erp-sidelink i {
        font-size: 1rem;
        width: 1.25rem;
        text-align: center;
        color: var(--sidebar-muted);
    }

    .erp-sidelink:hover {
        background: var(--sidebar-active);
    }

    .erp-sidelink[aria-current="page"],
    .erp-sidelink.active {
        background: #e5e7eb;
        font-weight: 600;
    }

/* Content kolom */
.erp-content {
    flex: 1 1 auto;
    background: var(--content-bg);
    padding: 1.25rem 1.25rem 2rem;
    min-width: 0; /* voorkomt overflow door lange teksten */
}

/* ============= Secties/Titels ============= */
.erp-title {
    font-family: "Segoe UI", system-ui, -apple-system;
    font-size: 2rem;
    font-weight: 800;
    margin: 1.5rem .5rem 1rem;
}

.erp-section {
    margin: 2rem .5rem;
}

/* ============= Cards/Icons ============= */
.erp-card {
    display: flex;
    align-items: center;
    gap: 1rem;
    background: var(--erp-card-bg);
    border: 1px solid var(--erp-card-border);
    border-radius: var(--erp-radius);
    padding: .9rem 1.25rem; /* compact voor vaste hoogte */

    color: var(--erp-text);
    text-decoration: none;
    box-shadow: var(--erp-shadow);
    transition: transform .15s ease-in-out, box-shadow .15s ease-in-out;
    min-height: var(--erp-card-min-h);
}

    .erp-card:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 24px rgba(0,0,0,.12);
    }

    /* Titel & body */
    .erp-card h3 {
        margin: 0 0 .25rem 0;
        font-size: 1.125rem;
        font-weight: 700;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    .erp-card p {
        margin: 0;
        color: var(--erp-muted);
        line-height: 1.25;
        /* clamp to 2 lines so height stays fixed */
        display: -webkit-box;
        -webkit-box-orient: vertical;
        -webkit-line-clamp: 2;
        overflow: hidden;
    }

    /* Icon-tegel */
    .erp-card .erp-icon {
        width: var(--icon-size);
        height: var(--icon-size);
        min-width: var(--icon-size);
        min-height: var(--icon-size);
        border-radius: var(--icon-radius);
        flex: 0 0 var(--icon-size);
        display: grid;
        place-items: center;
        line-height: 0;
        overflow: hidden;
        aspect-ratio: 1 / 1;
        /* reset */
        padding: 0 !important;
        border: 0 !important;
        margin: 0;
        font-size: 0;
        contain: layout paint size;
    }

.erp-icon > svg, .erp-icon > i, .erp-icon > img {
    width: 24px;
    height: 24px;
    display: block;
    line-height: 0;
    font-size: 24px;
}

/* Icon kleuren */
.erp-icon-orange {
    background: #f59e0b;
}

.erp-icon-amber {
    background: #f59e0b;
}

.erp-icon-green {
    background: #10b981;
}

.erp-icon-teal {
    background: #14b8a6;
}

.erp-icon-lime {
    background: #84cc16;
}

.erp-icon-blue {
    background: #3b82f6;
}

.erp-icon-cyan {
    background: #06b6d4;
}

.erp-icon-yellow {
    background: #fbbf24;
}

.erp-icon-pink {
    background: #ec4899;
}

.erp-icon-rose {
    background: #f43f5e;
}

.erp-icon-red {
    background: #ef4444;
}

.erp-icon-purple {
    background: #8b5cf6;
}

.erp-icon-violet {
    background: #7c3aed;
}

.erp-icon-indigo {
    background: #6366f1;
}

/* Focus states (toegankelijkheid) */
.erp-card:focus,
.erp-card:focus-visible {
    outline: 3px solid rgba(99,102,241,.35);
    outline-offset: 2px;
}

/* ============= Gelijke hoogte per rij (Bootstrap) ============= */
.row.equal-cards > [class*="col"] {
    display: flex;
}

.row.equal-cards .erp-card {
    flex: 1 1 auto;
}

/* ============= Helpers ============= */
/* Zorg dat hash-links (#inventaris etc.) niet onder de topbar vallen */
[id] {
    scroll-margin-top: calc(var(--topbar-h) + .5rem);
}

/* ============= Responsive tweaks ============= */
/* Sidebar: op <= md naar boven, onder elkaar */
@media (max-width: 991.98px) {
    .erp-sidebar {
        position: static; /* niet sticky op mobiel */
        width: 100%;
        flex: 1 1 100%;
        height: auto;
        border-right: 0;
        border-bottom: 1px solid var(--sidebar-border);
    }

    .erp-shell {
        flex-direction: column;
        min-height: auto;
    }

    .erp-content {
        padding: 1rem;
    }
}

/* Debug (optioneel) */
/* .erp-card .erp-icon { outline: 1px dashed #cbd5e1; } */
