/* ----------------------------
 Minimal ERP-styling (full)
 ---------------------------- */

*, *::before, *::after { box-sizing: border-box; }

/* Global reset for layout gaps */
html, body { height:100%; margin:0; padding:0; }

:root {
 --erp-card-bg: #f7f9fc;
 --erp-card-border: #eef2f7;
 --erp-text: #1f2937;
 --erp-muted: #6b7280;
 --erp-radius:14px;
 --erp-shadow:06px20px rgba(0,0,0, .06);
 --icon-size:56px;
 --icon-radius:12px;
 /* vaste minimale hoogte voor alle kaarten (pas aan naar wens) */
 --erp-card-min-h:120px;
 /* Sidebar (licht thema) */
 --sidebar-w:270px;
 --sidebar-bg: #f3f4f6; /* licht grijs */
 --sidebar-text: #111827; /* bijna zwart */
 --sidebar-muted: #6b7280; /* grijs */
 --sidebar-active: rgba(0,0,0,.06); /* hover highlight */
 --sidebar-border: #e5e7eb; /* subtiele rand */
 /* Topbar (donkerder) */
 --topbar-bg: #1f2937; /* donker grijs/blauw */
 --topbar-text: #f9fafb; /* licht */
 --topbar-text-muted: #e5e7eb;
 --topbar-height:56px;
}

/* Shell: remove all padding/margins and paint left column bg to avoid gaps */
.container-fluid.erp-shell { padding-left:0 !important; padding-right:0 !important; }
.erp-shell {
 display: flex;
 align-items: stretch; /* columns equal height */
 margin:0;
 padding:0 !important;
 background: var(--sidebar-bg); /* kleur ook achter de sidebar-kolom */
}

/* Sidebar fills full column height; inner nav sticks while scrolling */
.erp-sidebar {
 width: var(--sidebar-w);
 flex:00 var(--sidebar-w);
 background: var(--sidebar-bg);
 color: var(--sidebar-text);
 border-right:1px solid var(--sidebar-border);
 position: relative; /* geen sticky hier, zodat hoogte meerekt met content */
}

/* Maak alleen de inhoud sticky, niet het aside-element zelf */
.erp-sidebar .erp-brand { padding:1rem1.25rem; }
.erp-sidebar .erp-nav { position: sticky; top:0; padding: .5rem; }
/* Content area */
.erp-content { flex:11 auto; padding:1rem1.25rem; background: #fff; }

/* Topbar donker, zonder extra margin */
.erp-topbar { background-color: var(--topbar-bg) !important; color: var(--topbar-text) !important; margin-bottom:0 !important; }
.erp-topbar .navbar-brand, .erp-topbar .nav-link { color: var(--topbar-text) !important; }
.erp-topbar .nav-link:hover { color: var(--topbar-text-muted) !important; }


/* Titels & secties (cards blijven gelijk) */
.erp-title { font-family: "Segoe UI", system-ui, -apple-system; font-size:2rem; font-weight:800; margin:1.5rem; }
.erp-section { margin:2.5rem; }

/* Kaarten */
.erp-card { display: flex; align-items: center; gap:1rem; background: var(--erp-card-bg); border:1px solid var(--erp-card-border); border-radius: var(--erp-radius); padding: .875rem1.25rem; text-decoration: none; color: var(--erp-text); box-shadow: var(--erp-shadow); transition: transform .15s ease-in-out, box-shadow .15s ease-in-out; min-height: var(--erp-card-min-h); }
.erp-card:hover { transform: translateY(-2px); box-shadow:010px24px rgba(0,0,0, .12); }
.erp-card h3 { margin:00 .25rem0; font-size:1.125rem; font-weight:700; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.erp-card p { margin:0; color: var(--erp-muted); display: -webkit-box; -webkit-box-orient: vertical; -webkit-line-clamp:2; overflow: hidden; line-height:1.25; }

/* Icon-tegel (pixelvast + immuun voor externe styles) */
.erp-card .erp-icon { width: var(--icon-size); height: var(--icon-size); min-width: var(--icon-size); min-height: var(--icon-size); max-width: var(--icon-size); max-height: var(--icon-size); flex:00 var(--icon-size); padding:0 !important; border:0 !important; margin:0; line-height:0; font-size:0; overflow: hidden; display: grid; place-items: center; border-radius: var(--icon-radius); aspect-ratio:1 /1; contain: layout paint size; }

/* Inhoud (SVG/icon-font/IMG) consistent centreren en schalen */
.erp-icon > svg, .erp-icon > i, .erp-icon > img { width:24px; height:24px; display: block; line-height:0; font-size:24px; }

/* Kleuren voor iconen */
.erp-icon-orange { background: #f59e0b; }
.erp-icon-amber { background: #f59e0b; }
.erp-icon-green { background: #10b981; }
.erp-icon-teal { background: #14b8a6; }
.erp-icon-lime { background: #84cc16; }
.erp-icon-blue { background: #3b82f6; }
.erp-icon-cyan { background: #06b6d4; }
.erp-icon-yellow { background: #fbbf24; }
.erp-icon-pink { background: #ec4899; }
.erp-icon-rose { background: #f43f5e; }
.erp-icon-red { background: #ef4444; }
.erp-icon-purple { background: #8b5cf6; }
.erp-icon-violet { background: #7c3aed; }
.erp-icon-indigo { background: #6366f1; }

/* Toegankelijkheid: focus-states */
.erp-card:focus, .erp-card:focus-visible { outline:3px solid rgba(99,102,241, .35); outline-offset:2px; }

/* ----------------------------
 Gelijke hoogte per rij (Bootstrap)
 ---------------------------- */
.row.equal-cards > [class*="col"] { display: flex; }
.row.equal-cards .erp-card { flex:11 auto; }

/* (Optioneel) Debug: laat tijdelijk de tegelkaders zien */
/* .erp-card .erp-icon { outline:1px dashed #cbd5e1; } */
